<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: rgb(39, 42, 46);">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <%if(user==null||user==undefined) {%>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/login">Login</a>
          </li>
          <% } else { %>
            <li class="nav-item">
                <a class="nav-link active" href="/logout">Logout</a>
            </li>
            <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">Dashboard <span class="oi oi-dial"></span></a>
          </li>
        </ul>

        <%if(user==null||user==undefined) {%>
          <ul class="nav navbar-nav ml-auto">
            <li><a href="/login" class="text-white"><span class="oi oi-key text-light"></span> Login</a></li>
          </ul>
        <%} else {%>
          <div class="d-none d-sm-inline-block ml-auto flex-row" style="color:aliceblue;">
            <span style="margin-right: 10px;" class="d-flex">
              <strong style="color: rgb(254, 254, 254);"><%- user.username %></strong>#
              <%- user.discriminator %><a class="nav-link d-flex" href="#" id="userimg" role="button"><img class="ml-auto" src=<%= user.avatarURL %>?size=32></a></span>
          </div>
          <%}%>
        </div>
      </div>
    </div>
  </nav>